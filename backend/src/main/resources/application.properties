spring.application.name=diario-pessoal-backend

# ------------------------------------------------------------
# Banco de dados MySQL
# ------------------------------------------------------------
spring.datasource.url=jdbc:mysql://avnadmin:AVNS_gdXJ0H26Hu7gH6PoLUe@mysql-2c0f19fa-caiofelixmaia-6c16.d.aivencloud.com:23083/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=America/Sao_Paulo

# ------------------------------------------------------------
# JWT
# ------------------------------------------------------------
# Gere um valor forte, por exemplo: openssl rand -base64 64
app.jwt.secret=${c84a83693e5e49b800fd3ef17fa3b7ae906a5127d1a2eb345d1607ef1578f2cb}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}spring.application.name=diario-pessoal-backend

# ------------------------------------------------------------
# Banco de dados MySQL
# Em produção (Render), defina DB_URL com a connection string do
# Aiven, incluindo SSL (o Aiven exige conexão criptografada):
# jdbc:mysql://SEU-HOST.aivencloud.com:PORTA/defaultdb?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=America/Sao_Paulo&sslMode=REQUIRED
# ------------------------------------------------------------
spring.datasource.url=jdbc:mysql://avnadmin:AVNS_gdXJ0H26Hu7gH6PoLUe@mysql-2c0f19fa-caiofelixmaia-6c16.d.aivencloud.com:23083/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=America/Sao_Paulo

# ------------------------------------------------------------
# JWT
# ------------------------------------------------------------
# Gere um valor forte, por exemplo: openssl rand -base64 64
app.jwt.secret=${JWT_SECRET:troque-esta-chave-por-uma-secreta-de-producao-com-64-bytes-no-minimo}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ------------------------------------------------------------
# Upload de mídias (Cloudinary) - apenas URLs sao salvas no banco
# ------------------------------------------------------------
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# ------------------------------------------------------------
# Servidor
# ------------------------------------------------------------
server.port=${PORT:8080}
server.error.include-message=always

# ------------------------------------------------------------
# Actuator (para health check em produção)
# ------------------------------------------------------------
management.endpoints.web.exposure.include=health

# ------------------------------------------------------------
# Upload de mídias (Cloudinary) - apenas URLs sao salvas no banco
# ------------------------------------------------------------
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# ------------------------------------------------------------
# Servidor
# ------------------------------------------------------------
server.port=${PORT:8080}
server.error.include-message=always

# ------------------------------------------------------------
# Actuator (para health check em produção)
# ------------------------------------------------------------
management.endpoints.web.exposure.include=health
